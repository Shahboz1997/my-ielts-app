# DNS security records for stratumielts.com
# Add these in your domain registrar / DNS panel (Namecheap, Cloudflare, etc.)
# Cannot be applied from code — manual step required.
# Verify after 15–48h: https://mxtoolbox.com/SuperTool.aspx?action=spf%3astratumielts.com

# --- SPF (fixes "No SPF record found" in checkvibe) ---
# Type: TXT | Host: @ | TTL: 3600
# Option A — mail only from Gmail (current setup: supportstratum@gmail.com):
v=spf1 -all

# Option B — if you add Google Workspace (@stratumielts.com):
# v=spf1 include:_spf.google.com ~all

# --- DMARC (recommended after SPF) ---
# Type: TXT | Host: _dmarc | TTL: 3600
v=DMARC1; p=none; rua=mailto:supportstratum@gmail.com; fo=1

# After monitoring 2–4 weeks, tighten policy:
# v=DMARC1; p=quarantine; pct=100; rua=mailto:supportstratum@gmail.com

# --- DKIM ---
# Only needed when sending FROM @stratumielts.com (Google Workspace, Resend, etc.)
# Your email provider will give you CNAME/TXT records to paste here.
