# DNS security records for stratumielts.com (Vercel DNS: ns1.vercel-dns.com)
#
# Automated (recommended):
#   1. Create token: https://vercel.com/account/tokens
#   2. Run: $env:VERCEL_TOKEN="..."; node scripts/setup-vercel-dns.mjs
#
# Or CLI after `vercel login`:
#   vercel dns add stratumielts.com _dmarc TXT 'v=DMARC1; p=reject; pct=100; fo=1'
#   vercel dns add stratumielts.com @ MX . 0
#
# Verify after 15–48h: https://mxtoolbox.com/SuperTool.aspx?action=mx%3astratumielts.com

# --- SPF (fixes "No SPF record found" in checkvibe) ---
# Type: TXT | Host: @ | TTL: 3600
# Option A — mail only from Gmail (current setup: supportstratum@gmail.com):
v=spf1 -all

# Option B — if you add Google Workspace (@stratumielts.com):
# v=spf1 include:_spf.google.com ~all

# --- DMARC (recommended after SPF) ---
# Type: TXT | Host: _dmarc | TTL: 3600
v=DMARC1; p=reject; pct=100; fo=1

# No rua=mailto: — external mailboxes (e.g. gmail.com) trigger MXToolbox warnings
# unless that domain publishes DMARC report authorization for yours.

# --- DKIM ---
# Only needed when sending FROM @stratumielts.com (Google Workspace, Resend, etc.)
# Your email provider will give you CNAME/TXT records to paste here.
