'use client';

import GlowFollow from '@/components/GlowFollow';

/** Client effects on the server-rendered guest landing at `/`. */
export default function LandingMarketingClient() {
  return <GlowFollow />;
}
